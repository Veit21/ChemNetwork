random_seed: 42
train_loop_iterations: 50001
learning_rate: 1e-4
save_every: 5000
checkpoint_dir: "checkpoints"
verbose: False
comment: "default_comment"
device: "cpu" # choose from ["cpu", "cuda", "cuda:<idx>", "mps", "auto"]; falls back to "cpu" if the accelerator is unavailable